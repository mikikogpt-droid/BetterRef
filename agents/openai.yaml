interface:
  display_name: "BetterRef"
  short_description: "Match visual references with maximum fidelity."
  default_prompt: "Use BetterRef to match the provided reference image as closely as possible using all available tools."
