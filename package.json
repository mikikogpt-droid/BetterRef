{
  "name": "betterref",
  "version": "0.6.0",
  "description": "Reference-driven visual QA CLI for screenshots and UI images.",
  "type": "module",
  "private": false,
  "bin": {
    "betterref-eval": "./bin/betterref-eval.mjs",
    "betterref-guard": "./bin/betterref-guard.mjs",
    "betterref-verify": "./bin/betterref-verify.mjs",
    "betterref-chrome": "./bin/betterref-chrome.mjs",
    "betterref-prd": "./bin/betterref-prd.mjs",
    "betterref-diff": "./bin/betterref-diff.mjs",
    "betterref-capture": "./bin/betterref-capture.mjs",
    "betterref-regions": "./bin/betterref-regions.mjs"
  },
  "files": [
    "SKILL.md",
    "agents",
    "benchmarks",
    "bin",
    "lib",
    "references"
  ],
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "pdf-parse": "^2.4.5",
    "pixelmatch": "^7.1.0",
    "pngjs": "^7.0.0",
    "sharp": "^0.34.5",
    "ws": "^8.20.0"
  },
  "peerDependencies": {
    "playwright": ">=1.40.0"
  },
  "peerDependenciesMeta": {
    "playwright": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=20.16.0"
  },
  "devDependencies": {
    "pdfkit": "^0.18.0"
  }
}
