{
  "name": "betterref",
  "version": "0.5.0",
  "description": "Reference-driven visual QA CLI for screenshots and UI images.",
  "type": "module",
  "private": false,
  "bin": {
    "betterref-chrome": "./bin/betterref-chrome.mjs",
    "betterref-diff": "./bin/betterref-diff.mjs",
    "betterref-capture": "./bin/betterref-capture.mjs",
    "betterref-regions": "./bin/betterref-regions.mjs"
  },
  "files": [
    "SKILL.md",
    "agents",
    "bin",
    "lib"
  ],
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "pixelmatch": "^7.1.0",
    "pngjs": "^7.0.0",
    "sharp": "^0.34.5",
    "ws": "^8.20.0"
  },
  "peerDependencies": {
    "playwright": ">=1.40.0"
  },
  "peerDependenciesMeta": {
    "playwright": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=20"
  }
}
